proc mdc (m : int, n : int, r : @int) {
	#if(m % n == 0) {
		@r = n;
	#}
	#else {
#		if(n % m == 0){
#			return m;
#		}
#		else {
#			if(m > n){
#				@r = mdc(n, m%n);
#			}
#			else{
#				@r = mdc(m, n%m);
#			}
#		}
#	}	
}

main() {
	m : int;
	n : int;
	r : int;
	
	write ("Digite dois numeros inteiros: \n");
	m = readInt();
	n = readInt();

	mdc(m,n, $r);
	write ("O mdc de ");
	write (m);
	write (" e ");
	write (n);
	write (" é: ");
	write (r);

	write("\n");
	return 0;
}
